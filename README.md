# cl-wuffs

Common Lisp bindings for [Wuffs](https://github.com/google/wuffs).

Build the native shim with CMake, then load `cl-wuffs` through ASDF. See
[installation](docs/installation.md), [the API](docs/api.md), and
[updating generated bindings](docs/generation.md).
